server:
  port: 8082

spring:
  application:
    name: distributed-tx-seata-bank2
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        username: nacos
        password: nacos
        namespace: public
        file-extension: yml

# 防止nacos狂刷
logging:
  level:
    com.alibaba.nacos.client: error